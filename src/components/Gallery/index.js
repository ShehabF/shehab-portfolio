import Loader from 'react-loaders'
import './index.scss'

const Gallery = () => {
    return (
        <>
            <Loader type="ball-scale-ripple-multiple" />
        </>
    )
}

export default Gallery